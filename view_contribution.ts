export function show_contributions(){
    //
    //Speciy the sql for reading all the headers (from the events table)
    const sql = 'select name from event';
    //
    //Execute the sql to get the header data
    //
    //Get the header element
    //
    //Create a table row in the header
    //
    //Put the header elements into the row, so that it looks like
    /*
     <tr>
        <th>Name</th>
        <th>Githae</th>
        <th>Msafiri</th>
    </tr>
    */
    //
}